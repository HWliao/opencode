# Review package: Task 4 add-project

## Git status
 M packages/tui/src/app.tsx
 M packages/tui/test/fixture/tui-sdk.ts
?? packages/tui/src/component/dialog-project-add.tsx
?? packages/tui/src/component/dialog-project-list.tsx
?? packages/tui/test/component/dialog-project-add.test.ts

## Diff tracked files
diff --git a/packages/tui/src/app.tsx b/packages/tui/src/app.tsx
index d36d58c42..bcfb2c325 100644
--- a/packages/tui/src/app.tsx
+++ b/packages/tui/src/app.tsx
@@ -49,6 +49,8 @@ import { DialogHelp } from "./ui/dialog-help"
 import { DialogAgent } from "./component/dialog-agent"
 import { DialogSessionList } from "./component/dialog-session-list"
 import { DialogWorkspaceList } from "./component/dialog-workspace-list"
+import { DialogProjectAdd } from "./component/dialog-project-add"
+import { DialogProjectList } from "./component/dialog-project-list"
 import { DialogConsoleOrg } from "./component/dialog-console-org"
 import { ThemeProvider, useTheme } from "./context/theme"
 import { Home } from "./routes/home"
@@ -126,6 +128,7 @@ const appBindingCommands = [
   "help.show",
   "docs.open",
   "diff.open",
+  "project.list",
   "workspace.list",
   "app.debug",
   "app.console",
@@ -592,6 +595,17 @@ function App(props: { onSnapshot?: () => Promise<string[]>; pluginHost: TuiPlugi
           dialog.clear()
         },
       },
+      {
+        name: "project.list",
+        title: "Switch project",
+        category: "Project",
+        slashName: "projects",
+        run: () => {
+          const openList = () => dialog.replace(() => <DialogProjectList onOpenProject={openAdd} />)
+          const openAdd = () => dialog.replace(() => <DialogProjectAdd onBack={openList} />)
+          openList()
+        },
+      },
       {
         name: "workspace.copy_path",
         title: "Copy worktree path",
diff --git a/packages/tui/test/fixture/tui-sdk.ts b/packages/tui/test/fixture/tui-sdk.ts
index d1cf3c7df..c7ee10c59 100644
--- a/packages/tui/test/fixture/tui-sdk.ts
+++ b/packages/tui/test/fixture/tui-sdk.ts
@@ -86,8 +86,18 @@ export function createFetch(override?: FetchHandler, events?: ReturnType<typeof
     if (url.pathname === "/config/providers") return json({ providers: {}, default: {} })
     if (url.pathname === "/experimental/console") return json({ consoleManagedProviders: [], switchableOrgCount: 0 })
     if (url.pathname === "/experimental/capabilities") return json({ backgroundSubagents: false })
-    if (url.pathname === "/path") return json({ home: "", state: "", config: "", worktree, directory })
+    if (url.pathname === "/path")
+      return json({ home: "", state: "", config: "", worktree, directory: url.searchParams.get("directory") ?? directory })
     if (url.pathname === "/api/location") return json({ directory, project: { id: "proj_test", directory: worktree } })
+    if (url.pathname === "/directory") {
+      const target = url.searchParams.get("path") ?? directory
+      return json([
+        { name: "subA", absolute: `${target}/subA` },
+        { name: "subB", absolute: `${target}/subB` },
+      ])
+    }
+    if (url.pathname === "/project")
+      return json([{ id: "proj_test", worktree, name: "opencode", time: { created: 0, updated: 0 }, sandboxes: [] }])
     if (
       ["/api/agent", "/api/model", "/api/provider", "/api/integration", "/api/command", "/api/skill"].includes(
         url.pathname,
@@ -97,7 +107,8 @@ export function createFetch(override?: FetchHandler, events?: ReturnType<typeof
         location: { directory, project: { id: "proj_test", directory: worktree } },
         data: [],
       })
-    if (url.pathname === "/project/current") return json({ id: "proj_test" })
+    if (url.pathname === "/project/current")
+      return json({ id: "proj_test", worktree: url.searchParams.get("directory") ?? directory })
     if (url.pathname === "/api/reference")
       return json({ location: { directory, project: { id: "proj_test", directory } }, data: [] })
     if (url.pathname === "/provider") return json({ all: [], default: {}, connected: [] })

## New files
### packages/tui/src/component/dialog-project-add.tsx
+1: import { getDirectory } from "@opencode-ai/core/util/path"
+2: import { InputRenderable, TextAttributes } from "@opentui/core"
+3: import fuzzysort from "fuzzysort"
+4: import { createEffect, createMemo, createSignal, For, onCleanup, onMount, Show } from "solid-js"
+5: import { useProject } from "../context/project"
+6: import { useProjectSwitch } from "../context/project-switch"
+7: import { useSDK } from "../context/sdk"
+8: import { useTheme } from "../context/theme"
+9: import { useToast } from "../ui/toast"
+10: import { errorMessage } from "../util/error"
+11: 
+12: export type ProjectDirectoryEntry = { name: string; absolute: string }
+13: 
+14: export function normalizeProjectInput(input: string) {
+15:   const value = input.replaceAll("\\", "/")
+16:   if (/^[A-Za-z]:\/$/.test(value)) return value
+17:   return value.replace(/\/+$/g, "")
+18: }
+19: 
+20: export function projectParentDirectory(input: string) {
+21:   return normalizeProjectInput(getDirectory(normalizeProjectInput(input))).replace(/\/+$/g, "")
+22: }
+23: 
+24: function hasRoot(input: string) {
+25:   return input.startsWith("/") || input.startsWith("//") || /^[A-Za-z]:\//.test(input)
+26: }
+27: 
+28: export function joinProjectInput(base: string, input: string) {
+29:   const value = normalizeProjectInput(input.trim())
+30:   if (!value) return normalizeProjectInput(base)
+31:   if (hasRoot(value)) return value
+32:   return `${normalizeProjectInput(base)}/${value}`
+33: }
+34: 
+35: export function filterProjectDirectories(items: ProjectDirectoryEntry[], input: string) {
+36:   const query = normalizeProjectInput(input).split("/").at(-1) ?? ""
+37:   if (!query) return items
+38:   return fuzzysort.go(query, items, { key: "name" }).map((item) => item.obj)
+39: }
+40: 
+41: export function DialogProjectAdd(props: { onBack: () => void }) {
+42:   const sdk = useSDK()
+43:   const project = useProject()
+44:   const switchProject = useProjectSwitch()
+45:   const toast = useToast()
+46:   const { theme } = useTheme()
+47:   const [input, setInput] = createSignal(projectParentDirectory(project.instance.directory() || sdk.directory || ""))
+48:   const [browseRoot, setBrowseRoot] = createSignal(input())
+49:   const [items, setItems] = createSignal<ProjectDirectoryEntry[]>([])
+50:   const [selected, setSelected] = createSignal(0)
+51:   const [debouncedInput, setDebouncedInput] = createSignal(input())
+52:   let inputRef: InputRenderable | undefined
+53: 
+54:   const filtered = createMemo(() => filterProjectDirectories(items(), debouncedInput()))
+55: 
+56:   createEffect(() => {
+57:     const next = input()
+58:     const timer = setTimeout(() => setDebouncedInput(next), 10)
+59:     onCleanup(() => clearTimeout(timer))
+60:   })
+61: 
+62:   createEffect(() => {
+63:     filtered()
+64:     setSelected(0)
+65:   })
+66: 
+67:   async function load(path: string) {
+68:     const next = normalizeProjectInput(path)
+69:     const response = await sdk.client.directory.list({ path: next })
+70:     setBrowseRoot(next)
+71:     setInput(next)
+72:     if (inputRef) inputRef.value = next
+73:     setItems(response.data ?? [])
+74:     setSelected(0)
+75:   }
+76: 
+77:   function move(delta: number) {
+78:     const count = filtered().length
+79:     if (count === 0) return
+80:     setSelected((index) => (index + delta + count) % count)
+81:   }
+82: 
+83:   async function chooseSelected() {
+84:     const item = filtered()[selected()]
+85:     if (!item) return
+86:     await load(item.absolute)
+87:   }
+88: 
+89:   async function confirm() {
+90:     await switchProject(normalizeProjectInput(input()))
+91:   }
+92: 
+93:   onMount(() => {
+94:     void load(input()).catch((error) =>
+95:       toast.show({ variant: "error", title: "Failed to list directories", message: errorMessage(error) }),
+96:     )
+97:   })
+98: 
+99:   return (
+100:     <box gap={1} paddingBottom={1} flexGrow={1}>
+101:       <box paddingLeft={4} paddingRight={4}>
+102:         <box flexDirection="row" justifyContent="space-between">
+103:           <text fg={theme.text} attributes={TextAttributes.BOLD}>
+104:             Open Project
+105:           </text>
+106:           <text fg={theme.textMuted}>
+107:             esc
+108:           </text>
+109:         </box>
+110:       </box>
+111:       <box paddingX={4} paddingBottom={1} flexDirection="column" gap={1}>
+112:         <input
+113:           focusedBackgroundColor={theme.backgroundPanel}
+114:           cursorColor={theme.primary}
+115:           focusedTextColor={theme.text}
+116:           ref={(element) => {
+117:             inputRef = element
+118:             inputRef.value = input()
+119:             setTimeout(() => {
+120:               if (!inputRef || inputRef.isDestroyed) return
+121:               inputRef.focus()
+122:             }, 1)
+123:           }}
+124:           onInput={(value) => setInput(joinProjectInput(browseRoot(), value))}
+125:           onKeyDown={(event) => {
+126:             if (event.name === "escape") props.onBack()
+127:             if (event.name === "up") move(-1)
+128:             if (event.name === "down") move(1)
+129:             if (event.name === "space") void chooseSelected().catch(toast.error)
+130:             if (event.name === "return") void confirm().catch(toast.error)
+131:           }}
+132:         />
+133:         <Show when={filtered().length} fallback={<text fg={theme.textMuted}>No directories found</text>}>
+134:           <For each={filtered()}>
+135:             {(item, index) => (
+136:               <text
+137:                 fg={index() === selected() ? theme.primary : theme.text}
+138:                 attributes={index() === selected() ? TextAttributes.BOLD : undefined}
+139:               >
+140:                 {item.absolute}
+141:               </text>
+142:             )}
+143:           </For>
+144:         </Show>
+145:       </box>
+146:     </box>
+147:   )
+148: }

### packages/tui/test/component/dialog-project-add.test.ts
+1: import { describe, expect, test } from "bun:test"
+2: import {
+3:   filterProjectDirectories,
+4:   joinProjectInput,
+5:   normalizeProjectInput,
+6:   projectParentDirectory,
+7: } from "../../src/component/dialog-project-add"
+8: 
+9: describe("dialog project add", () => {
+10:   test("starts from the current project parent directory", () => {
+11:     expect(projectParentDirectory("D:/develop/opencode/lucky-cactus")).toBe("D:/develop/opencode")
+12:     expect(projectParentDirectory("/home/leo/opencode/lucky-cactus")).toBe("/home/leo/opencode")
+13:   })
+14: 
+15:   test("typing appends to the current directory input", () => {
+16:     expect(joinProjectInput("D:/develop/opencode", "A")).toBe("D:/develop/opencode/A")
+17:     expect(joinProjectInput("D:/develop/opencode", "D:/other/repo")).toBe("D:/other/repo")
+18:     expect(joinProjectInput("D:/develop/opencode", "D:\\other\\repo")).toBe("D:/other/repo")
+19:   })
+20: 
+21:   test("filters direct child directories with fuzzy matching", () => {
+22:     const items = [
+23:       { name: "subA", absolute: "D:/develop/opencode/subA" },
+24:       { name: "subB", absolute: "D:/develop/opencode/subB" },
+25:     ]
+26:     expect(filterProjectDirectories(items, "D:/develop/opencode/A").map((item) => item.absolute)).toEqual([
+27:       "D:/develop/opencode/subA",
+28:     ])
+29:   })
+30: 
+31:   test("normalizes windows separators for display and comparison", () => {
+32:     expect(normalizeProjectInput("D:\\develop\\opencode\\")).toBe("D:/develop/opencode")
+33:   })
+34: })

